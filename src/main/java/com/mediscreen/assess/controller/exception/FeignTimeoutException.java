/*
package com.mediscreen.assess.controller.exception;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.ResponseStatus;

@ResponseStatus(HttpStatus.REQUEST_TIMEOUT)
public class FeignTimeoutException extends Exception {
    private static final Logger logger = LogManager.getLogger(FeignTimeoutException.class);
    public FeignTimeoutException(String s) {
        super(s);
        logger.error(s);
    }
}
*/
