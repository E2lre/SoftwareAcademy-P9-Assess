/*
package com.mediscreen.assess.controller.exception;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;
import org.springframework.http.HttpStatus;
import org.springframework.web.bind.annotation.ResponseStatus;

@ResponseStatus(HttpStatus.INTERNAL_SERVER_ERROR)
public class FeignErrorException extends Exception {
    private static final Logger logger = LogManager.getLogger(FeignErrorException.class);
    public FeignErrorException(String s) {
        super(s);
        logger.error(s);
    }
}
*/
